# Prompt — GitHub Pages fallback (Lab 08)

ใช้เมื่อ Coolify / DNS ยังไม่พร้อม — **ต้องบอกวิทยากร**  
คัดลอกเฉพาะบล็อกด้านล่าง

```text
ช่วยตั้งค่า static export (ถ้า template รองรับ) + GitHub Actions deploy Pages

อธิบายชัดว่า guestbook API ไม่ทำงานบน static host
เกณฑ์ผ่าน fallback: มี URL Pages ที่ตอบ 200 + หน้า 4 หลักครบ
บันทึกใน docs/SHIP.md ว่าเป็น fallback + URL + ข้อจำกัด API

อย่าเคลมว่า Coolify / slug.9expert.online สำเร็จถ้ายังไม่ได้ deploy ที่นั่น
```
