# Prompt — Merge หลัง /init (Lab 00 · Claude)

ใช้เมื่อ `/init` เสนอเขียน `CLAUDE.md` ใหม่ทั้งไฟล์  
คัดลอกเฉพาะบล็อกด้านล่าง

```text
มี CLAUDE.md seed ของคอร์สอยู่แล้ว — ห้ามลบหรือทับส่วนเหล่านี้:
- Ownership: Frontend/UI = Claude Code · Backend/API+SQLite = OpenCode
- ห้ามใช้ MCP เป็นท่อส่งงานระหว่าง Claude ↔ OpenCode
- Cross-CLI เฉพาะ Lab 07 สำหรับรีวิว
- ทำงานที่ root ของ repo (มี package.json)
- ห้าม commit .env / ความลับ · PR เข้า learner repo เท่านั้น

งาน: merge โครงสร้าง/คำสั่งที่มีประโยชน์จาก /init เข้ากับ seed
เก็บภาษาไทยหรือสองภาษาได้ แต่กฎคอร์สด้านบนต้องคงอยู่
อย่าแตะ .env หรือ node_modules
```
