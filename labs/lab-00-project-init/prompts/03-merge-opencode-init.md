# Prompt — Merge หลัง /init (Lab 00 · OpenCode)

ใช้เมื่อ `/init` เสนอเขียน `AGENTS.md` ใหม่ทั้งไฟล์  
คัดลอกเฉพาะบล็อกด้านล่าง

```text
มี AGENTS.md seed ของคอร์สอยู่แล้ว — ห้ามลบตาราง Ownership และกฎ Native harness only

คงไว้:
- UI pages = Claude Code · API/SQLite = OpenCode
- MCP ไม่ใช่ท่อระหว่างสอง CLI
- Cross-CLI เฉพาะ Lab 07
- ห้าม commit ความลับ · PR เข้า learner repo เท่านั้น

merge คำสั่ง npm / โครงสร้างโฟลเดอร์ที่ /init แนะนำเข้าไปได้
อย่าแก้ src/ ในรอบนี้
```
